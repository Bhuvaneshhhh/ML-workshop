import numpy as np
arr=np.array([1, 2, 3, 4, 5])

sqrt_arr = np.sqrt(arr)
print(sqrt_arr)

exp_arr = np.exp(arr)
print(exp_arr)

sin_arr = np.sin(arr)
print(sin_arr)

sum_arr = np.sum(arr)
print(sum_arr)

mean_arr = np.mean(arr)
print(mean_arr)

